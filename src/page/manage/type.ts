export interface manageEmployeeProps {
  employeeList: {
    name: string;
    backgroundColor: string;
    rank: string;
  }[];
}

export interface manageWorkProps {
  workFormList: {
    workForm: string;
    time: string;
  }[];
}
